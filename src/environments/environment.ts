// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

// export const URL_SERVICES = 'https://rest-api-ecommerce-production.up.railway.app/api/';
export const URL_SERVICES = 'http://127.0.0.1:4201/api/';

// export const SOCKET = 'https://rest-api-ecommerce-production.up.railway.app';
export const SOCKET = 'http://localhost:4201';

export const URL_TIENDA = 'http://localhost:4300/';

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
